import {StyleSheet} from '@components';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 6,
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  iconContainer: {
    flexDirection: 'row',
    padding: 6,
    width: 50,
    justifyContent: 'space-between',
  },
});
